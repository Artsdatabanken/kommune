# Bilder av kommuner
SELECT ?kommune ?image ?banner ?flag WHERE {
    ?kommune wdt:P31 wd:Q755707;
      wdt:P18 ?image.
    OPTIONAL {
      ?kommune wdt:P948 ?banner.
    }
    OPTIONAL { ?kommune wdt:P41 ?flag. }
  }
ORDER BY ?kommune
